# Projects

Keep active project folders and their current source files here.

Start with ordinary folders. Create separate repositories only when permissions,
deployment, collaborators, or release history genuinely need to be separate.

